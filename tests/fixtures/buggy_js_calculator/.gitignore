# Running the suite inside the sandbox creates these next to the source.
# Without ignoring them, the orchestrator's `git add -A` would sweep them
# into the fix commit and the Reviewer's scope check would (correctly!)
# flag the diff as touching files the root cause never named.
node_modules/
*.log
